#include <stdio.h>
int main() {
    double x, y, c;
    printf("Enter two numbers: ");
    scanf("%lf %lf", &x, &y);  
 
    // Multiplying x, y and storing it into c
    c = x * y;

    // %.2lf displays number up to 2 decimal point
    printf("Product = %.2lf", c);
    
    return 0;
}
